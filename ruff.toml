target-version = "py312"

[lint]
# Keep CI on Ruff's established correctness baseline. Explicit selection avoids
# inheriting a runner-level configuration when linting from the repository root.
select = ["E4", "E7", "E9", "F"]
