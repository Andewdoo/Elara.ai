# Elara Web App

Next.js App Router frontend for Elara.ai.

This package will contain browser routes, UI components, hooks, transient UI stores, Firebase Web authentication, and API access through FastAPI. Browser code must not call DeepSeek, Brave Search, PostgreSQL, Redis, object storage, or other privileged services directly.

