###

You are working in C:\Users\aliua\Elara.ai. Read AGENTS.md and IMPLEMENTATION_PLAN.md before changing code. Follow the fixed stack exactly: Next.js App Router, TypeScript, Tailwind, shadcn/ui, FastAPI, SQLAlchemy, Alembic, PostgreSQL/pgvector, Redis, Celery, LangGraph, DeepSeek API, Firebase Authentication, Brave Search, httpx, Trafilatura, Beautiful Soup, PyMuPDF, React Flow, Recharts, Sentry, and LangSmith-compatible tracing where useful. Do not use OpenAI APIs, OpenAI env vars, Firebase Hosting, Firestore, Firebase Storage, Firebase Functions, browser-side model calls, browser-side search calls, browser-side database calls, or permanent public object-storage URLs. Keep DeepSeek, search, Firebase Admin, database, Redis, object-storage, Sentry auth, and LangSmith credentials server-side only. Treat retrieved content as untrusted evidence. Keep scoring deterministic. Implement the requested step end to end, add focused tests where appropriate, run available checks, and summarize changed files plus verification.

Run Step x.
Then ask: review Step x for gaps against AGENTS.md and IMPLEMENTATION_PLAN.md, fix anything missing, run checks.
Then Step x+1.
Repeat.

dev server startup: npm run --workspace @elara/web dev -- --port 3000

###

Step 1: Create the monorepo structure. Build the folders from IMPLEMENTATION_PLAN.md: apps/web, apps/api, apps/worker, packages/api-client, packages/shared-types, packages/design-tokens, evals, infrastructure. Add sensible starter README files or package placeholders only where needed. Add root config files such as .gitignore and .env.example with placeholder-only variables for app URLs, CORS, DeepSeek, Brave Search, Firebase Web config, Firebase Admin config, database, Redis, S3-compatible object storage, Sentry DSNs/auth metadata, and LangSmith tracing. Include only the Firebase variables used by the plan: NEXT_PUBLIC_FIREBASE_API_KEY, NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN, NEXT_PUBLIC_FIREBASE_PROJECT_ID, NEXT_PUBLIC_FIREBASE_APP_ID, FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY. Include SEARCH_PROVIDER=brave, SEARCH_API_KEY, and SEARCH_BASE_URL=https://api.search.brave.com/res/v1; do not include SEARCH_ENGINE_ID for Brave. Do not add VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID, container-registry credentials, Firebase Storage bucket, Firebase Messaging sender id, or Firebase Auth emulator variables for the default deployment path. Add docker-compose.yml scaffold for PostgreSQL with pgvector, Redis, S3-compatible storage, FastAPI, Celery worker, and web service wiring. Do not add unsupported frameworks. Keep the structure ready for later implementation.

Step 2: Build the Next.js application shell, routes, design system, and mocked report interface. Use Next.js App Router, TypeScript, Tailwind CSS, shadcn/ui style components, React Hook Form, Zod, TanStack Query, Zustand for transient UI only, Lucide icons, React Flow and Recharts dependencies/hooks where needed. Create routes for /, /methodology, /verify, /verify/[runId], /report/[runId], /history, /saved, and /settings. Add a Firebase Auth provider shell that uses only the four public NEXT*PUBLIC_FIREBASE*\* values; do not expose Firebase Admin, DeepSeek, search, database, Redis, object-storage, Sentry auth token, or LangSmith credentials to the browser. Build a usable app-first interface, not a marketing landing page. Include mocked report data showing verdict, scores, atomic claims, evidence, inaccessible sources, calculations, methodology/version details, and the required "Evidence reviewed as of..." timestamp. Keep chart data mocked but shaped like API records, not browser recomputation.

Step 3: Build Firebase Web sign-in, FastAPI Firebase Admin token/session verification, Pydantic verification schemas, and PostgreSQL user/run persistence. Create apps/api app structure with routes, auth, database, models, schemas, services, tests, plus the matching web auth helpers/providers in apps/web. Implement config loading with server-side env vars and no OpenAI names. In Next.js, sign in and sign out with the Firebase Web SDK using only public NEXT*PUBLIC_FIREBASE*\* values. In FastAPI, verify Firebase ID tokens with the Firebase Admin SDK, load or create the PostgreSQL users row with auth_provider="firebase" and auth_subject set to the Firebase uid, and enforce ownership-aware service boundaries. Add POST /v1/auth/session to exchange a fresh Firebase ID token for a short-lived HttpOnly Secure Firebase session cookie for credentialed SSE, and DELETE /v1/auth/session to clear it. Normal API requests should use Authorization: Bearer <Firebase ID token>; SSE must use the session cookie and never put tokens in URLs. Implement Pydantic schemas for verification create, progress event, report response, score bundle, atomic claim, evidence item, source graph, and calculation response from IMPLEMENTATION_PLAN.md. Implement POST /v1/verifications to authenticate, validate, create a durable QUEUED run, write first agent_event, commit, and return 202 with run_id/events_url without doing retrieval or model work.

Step 4: Add Alembic migrations for users, runs, claims, sources, snapshots, passages, evidence, calculations, events, versions, and pgvector. Implement SQLAlchemy models and enums for RunStatus, InputType, ResearchDepth, SourceType, AccessStatus, EvidenceStance, DependencyRelationship. Enable vector and citext extensions. Use UUID primary keys, timezone-aware timestamps, explicit indexes, ownership indexes, status/timestamp indexes, graph query indexes, and downgrade logic where practical. Include source_snapshots versioning rules, source_passages embedding vector column with configurable dimension, report_citations, user_feedback, methodology_versions, exports, and calculations with Decimal audit metadata. Add tests or migration checks proving metadata imports and migrations are coherent.

Step 5: Add Redis, Celery, run queues, cancellation flags, and Redis Stream progress. Create Celery app in worker/API shared config as appropriate. Configure queues verification.quick, verification.standard, verification.deep. Implement task signature verification.verify_run(run_id) with retry behavior for transient provider/fetch errors. Route tasks by ResearchDepth. Add Redis helpers for progress streams elara:run:{run_id}:events, fetch locks, run locks, rate limit keys, and cache key helpers. Worker must load durable run state from PostgreSQL, transition statuses durably, mirror public events to agent_events, write Redis progress, check cancellation before expensive stages, and mark failures with concise public codes. No private reasoning in events.

Step 6: Add SSE endpoint and frontend live research view. Implement GET /v1/verifications/{run_id}/events using FastAPI StreamingResponse text/event-stream. Authenticate with the Firebase session cookie, authorize the run against PostgreSQL ownership or share policy, and never accept Firebase tokens in the SSE URL. Read Redis Stream progress, support Last-Event-ID replay, send heartbeat comments every 15-30 seconds, and end after COMPLETED, FAILED, or CANCELLED. Add frontend useRunEvents(runId) with EventSource, withCredentials enabled, reconnect handling, terminal-status cache invalidation, and polling fallback. Ensure CORS allows only configured frontend origins and credentials. Build live research UI with current stage, completed/total steps, source counts, inaccessible notices, cancellation button, elapsed time, and latest public event. Final status/report must reload from PostgreSQL via TanStack Query.

Step 7: Implement DeepSeek client wrapper and structured Pydantic agent outputs. Create apps/worker/agents/deepseek_client.py named DeepSeekClient. Use httpx.AsyncClient directly. Read DEEPSEEK_API_KEY, DEEPSEEK_BASE_URL, DEEPSEEK_CHAT_MODEL, DEEPSEEK_REASONING_MODEL, optional DEEPSEEK_EMBEDDING_MODEL from server-side env only. Support structured JSON outputs, low-temperature calls, provider error mapping, latency/token/model/prompt-version metadata, and safe logging with prompt/source redaction. Add Pydantic schemas for intake classification, decomposition, planning, evidence classification, synthesis, and citation audit outputs. Add tests for env validation, error handling, JSON parsing, and no OpenAI env/provider references.

Step 8: Implement LangGraph workflow nodes for intake, decomposition, planner, evidence classification, synthesis, and citation audit. Create worker graph state.py, workflow.py, transitions.py. State must be typed with Pydantic and include run_id, user_id, normalized input, research depth, methodology version, claims, objectives, queries, candidate sources, snapshots, passages, evidence, dependencies, calculations, scores, report draft, citation audit, and recoverable errors. Each node must accept/return typed state, persist public progress, check cancellation, avoid private chain-of-thought storage, and record recoverable failures. Implement intake, decomposition, planner, evidence classification, synthesis, and citation audit using DeepSeek only for language understanding and deterministic guards for validation. Leave clear typed extension points for the full AGENTS.md workflow: discovery/source selection, secure retrieval, extraction, passage segmentation/embedding, provenance/dependency analysis, deterministic scoring, and numerical audit.

Step 9: Implement targeted retrieval: Brave Search provider, URL guard, cache, httpx fetcher, Trafilatura extraction, Beautiful Soup fallback, and PyMuPDF PDF parsing. Create research/search.py, ranking.py, url_guard.py, fetcher.py, cache.py and extraction modules. Use SEARCH_PROVIDER=brave, SEARCH_API_KEY, and SEARCH_BASE_URL for the search client; do not require SEARCH_ENGINE_ID for Brave and never call search providers from the browser. URL guard must allow only HTTP/HTTPS, reject localhost/private/link-local/reserved/cloud metadata IPs, re-check DNS at connection time, revalidate redirects, restrict ports, enforce timeouts and size limits, reject executable/unsupported content, never forward user cookies or credentials, and store fetched files outside executable paths. Extraction fallback order: httpx+Trafilatura, Beautiful Soup, Playwright placeholder/future fallback, PyMuPDF for PDFs, then mark inaccessible. Persist sources/snapshots/run_sources with explicit access status.

Step 10: Add passage segmentation, hashing, embedding generation through approved DeepSeek-compatible route, and pgvector search. Segment extracted content by headings, paragraphs, transcript turns, table rows, and semantic boundaries with limited overlap. Preserve page/position, heading path, paragraph index, speaker, table refs, source/snapshot ids, exact text, and text hash. Implement embedding interface that uses only approved DeepSeek-compatible embedding route; if unavailable, keep pgvector schema and provide lexical/metadata retrieval fallback. Store embedding_model per passage and model versions per run. Implement retrieval combining lexical score, vector similarity when available, exact quote/number/identifier match, metadata fit, source role, and extraction certainty. Vector search assists retrieval only; it must not decide truth.

Step 11: Add source provenance, dependency clustering, and source graph API. Implement provenance/dependencies.py, clustering.py, graph_export.py. Detect explicit citations/outbound links, syndication, near-identical text, shared quotations, shared statistics/tables/errors, timestamps, and first-known appearances. Store information_clusters and source_dependencies with CITES, REPUBLISHES, QUOTES, DERIVES_FROM, USES_SAME_DATA, POSSIBLE_DUPLICATE. Apply dependency multipliers: independent 1.00, meaningful independent analysis using the same chain 0.35, repeated second-hand reporting 0.10, exact syndicated/copy 0.00. Add GET /v1/verifications/{run_id}/source-graph returning nodes/edges suitable for React Flow with authorization.

Step 12: Add deterministic evidence quality, support, confidence, independence, quote fidelity, context, and label services. Implement pure Python scoring modules. Evidence stance may come from model classification, but all final arithmetic and labels must be deterministic. Use formulas from IMPLEMENTATION_PLAN.md: q_i weighted quality, adjusted weight w_i, supporting P, contradicting N, evidence support, consistency, verdict confidence, source independence, quote fidelity, context completeness, article factual accuracy, essential-claim gate, and insufficient-evidence gate. Use Decimal where precision matters. Persist calculation records with formula_name, formula_text, inputs, result, units, decimal_context, and audit_status. Add focused tests for formula correctness and gates.

Step 13: Add numerical auditing with Decimal. Build auditing/numerical.py. The model may identify candidate values and formulas, but deterministic code must validate and compute percentages, ratios, totals, comparisons, unit conversions, denominators, and reporting periods using Python Decimal. Store every reproducible calculation in calculations. Handle missing denominators, mismatched units, period mismatches, rounding rules, and source-value conflicts explicitly. Add tests for Decimal reproducibility, unit conversion, percentage/ratio correctness, denominator validation, and failure cases. Reports should expose calculation inputs and audit status, not opaque conclusions.

Step 14: Add report workspace, source drawer, React Flow graph, and Recharts score visualizations. Replace mocked report UI with API-backed TanStack Query data for run, report, sources, source graph, history/exports where available. Build desktop layout with left atomic-claim rail, center overview/evidence panels, right source drawer. Build mobile tabs for overview, claims, evidence, graph, calculations, methodology. Source drawer shows exact passage, metadata, snapshot/citation details, access status, parser/version, and retrieval time. React Flow graph uses source graph API. Recharts visualizations show score breakdown, evidence balance, confidence components, research coverage, and numerical audit using server calculation records only.

Step 15: Add feedback, corrections, exports, saved reports, and history. Implement GET /v1/history, POST /v1/verifications/{run_id}/feedback, POST /v1/verifications/{run_id}/exports, GET /v1/verifications/{run_id}/exports/{export_id}, save/delete report behavior as designed. Enforce ownership/share authorization for every route. Store feedback categories for corrections, missed evidence, appeals, and broken citations. Generate JSON export first, PDF export if practical with existing tooling, store in S3-compatible object storage with content_hash and no permanent public URLs. Issue short-lived signed URLs only after authorization. Frontend should include history filters, saved reports page, feedback/correction controls, and export controls.

Step 16: Add Sentry, LangSmith-compatible worker tracing, worker metrics, provider usage metrics, and evaluation harness. Configure Sentry separately for web, API, and worker using NEXT_PUBLIC_SENTRY_DSN, SENTRY_DSN_API, SENTRY_DSN_WORKER, SENTRY_ORG, SENTRY_PROJECT_WEB, SENTRY_PROJECT_API, SENTRY_PROJECT_WORKER, and SENTRY_AUTH_TOKEN where needed. Configure LangSmith tracing with LANGSMITH_TRACING, LANGSMITH_API_KEY, LANGSMITH_PROJECT, and optional LANGSMITH_ENDPOINT, but keep it server-side and attach DeepSeek model/provider metadata to traces rather than treating DeepSeek as a LangSmith SDK. Add observability without leaking secrets, full private uploads, prompts, or sensitive retrieved content. Track search-to-fetch conversion, extraction success, median fetch latency, Playwright fallback rate, cache hit rate, duplicate-cluster rate, evidence yield, cost per verification, DeepSeek token usage, source accessibility failure rate, citation-audit failure rate, queue length, run duration, retry count, and cancellation rate. Build evals structure for verdict accuracy, attribution accuracy, evidence precision/recall, citation entailment, extraction fidelity, primary-source recall, duplicate clustering, confidence calibration, and unsupported-statement rate. Add GitHub Actions scaffold for type checks, tests, builds, migration checks, eval smoke tests, and staging/prod deployment gates. Vercel should deploy the Next.js app from the GitHub-connected repo; do not add Vercel tokens or container-registry credentials unless the deployment architecture explicitly changes.

Step 17: Add security regression tests and production hardening. Create high-priority tests for URL guard SSRF cases, redirects to private IPs, DNS rebinding-style revalidation, response size enforcement, upload validation, unsupported executable files, hostile HTML, prompt injection content, run authorization, cross-user access prevention, Firebase token/session verification, Celery enqueue/status transitions, SSE reconnect behavior without URL tokens, scoring formula correctness, Decimal numerical audits, citation audit rejection, source dependency multipliers, inaccessible-source handling, and DeepSeek client error handling. Harden config defaults, exact-origin CORS, security headers, Firebase authorized-domain notes, HttpOnly Secure cookie/auth settings, rate limits, timeout defaults, logging redaction, object-storage URL issuance, migration checks, and deployment docs for Vercel plus a GitHub-connected backend/worker host. Ensure final checklist in AGENTS.md is satisfied.

Step 18: Close the production verification workflow end to end. Fix the runtime so verification.verify_run executes discovery, secure retrieval, extraction, passage segmentation/embedding, provenance, evidence classification, deterministic scoring, numerical audit, synthesis, and citation audit rather than stopping after numerical audit. Rename planning-only runtime names where appropriate without breaking imports unnecessarily. Implement a bounded evidence-grounded citation revision and re-audit loop; unsupported or partially supported factual sentences must be removed or revised using approved passages only. If revision remains necessary after the configured limit, fail safely with a concise public code. Persist all report and citation artifacts before using the deterministic completion gate, then emit a durable run.completed event and transition to COMPLETED. Mirror the terminal event to Redis for SSE while keeping PostgreSQL authoritative. Make task retry, redelivery, cancellation races, and already-terminal runs idempotent. Add production-runtime tests proving the complete status sequence, report availability after completion, export authorization, citation failure rejection, cancellation, retry, and no duplicate durable records. Fix the duplicate TestTransientStore.expire definition and run focused checks.

Step 19: Close every quality gate. Run the exact GitHub Actions commands and working directories for Ruff, focused mypy, API tests, worker tests, evaluation tests, frontend lint, TypeScript checks, frontend tests, optimized Next.js build, Alembic single-head and offline upgrade/downgrade SQL checks, security gates, and API/worker container builds. Fix every failure rather than documenting around it unless the user explicitly accepts a narrowly justified exception. Add missing regression coverage where a test-only workflow path differs from the production runtime. Verify from a clean checkout configuration with no real credentials, and summarize each gate with command, result, and any intentional limitation.

Step 20: Add a deterministic full-stack end-to-end acceptance environment. Use PostgreSQL with pgvector, Redis, S3-compatible private object storage, FastAPI, Celery, and deterministic local test doubles for DeepSeek and Brave so CI needs no provider credentials. Exercise authentication, verification submission, queue dispatch, credentialed SSE without URL tokens, controlled discovery/retrieval/extraction, snapshot and passage persistence, provenance, evidence, calculations, synthesis, citation audit, durable COMPLETED, report/source/graph/calculation reload, authorized private export, and cross-user denial. Simulate browser refresh, Redis loss/reconnect, task redelivery, provider failure, cancellation, and citation rejection. Keep the test deterministic and suitable for CI.

Step 21: Replace the explicit Playwright placeholder with a production-safe isolated fallback. Static httpx plus Trafilatura and Beautiful Soup must remain first. Use Playwright only for important JavaScript-rendered sources after static extraction fails. Reapply URL, DNS, IP, port, redirect, timeout, and size policy to every navigation and subrequest. Use a fresh browser context with no user cookies or provider credentials; block downloads, popups, unnecessary media, and nonessential third-party requests; limit navigation, total time, response size, DOM size, and retries. Record fallback reason, parser version, extraction certainty, and inaccessible status. Add regression tests for JavaScript-only pages, malicious redirects, subresource SSRF, oversized DOMs, hostile HTML, malformed PDFs, page-aware citations, paywalls, correction notices, changed snapshots, cache reuse, and fetch locks. Preserve partial Brave results and bounded transient retries; do not add a second search provider without explicit architecture approval.

Step 22: Build the human-reviewed evaluation and calibration program. Expand beyond the two-case smoke fixture with approved benchmark cases covering finance, science, technology, current events, direct quotes, paraphrases, allegations, numerical claims, misleading context, and insufficient evidence. For every case record atomic claims, importance, primary sources, acceptable passages, support and contradiction, attribution expectations, expected labels and calculations, citation-entailment judgments, dependency clusters, ambiguities, and inaccessible-source expectations. Separate development/calibration, locked validation, adversarial-security, and regression sets. Measure verdict macro-F1, attribution accuracy, evidence precision/recall, passage recall, primary-source recall, citation entailment, extraction fidelity, numerical accuracy, source clustering, unsupported-statement rate, confidence calibration/Brier score, latency, and cost. Version every formula/threshold change and rerun locked validation. Add a versioned evaluation report and block public launch until reviewed release thresholds are documented and met; unsupported factual statements must be zero in the release benchmark.

Step 23: Complete product and report acceptance. Audit all routes and components for desktop, mobile, keyboard, loading, empty, failure, reconnect, retry, and cancellation behavior. Remove remaining production-facing mock surfaces. Verify that every applicable report exposes distinct score roles, required evidence timestamp, limitations, inaccessible sources, strongest credible contradiction, supporting and contradicting passages, page/section/timestamp positions, attribution separate from factual content, calculation inputs and audit status, source dependencies, and methodology/workflow/prompt/model/parser/retrieval/source versions. Verify feedback, correction, saved-report, history, and authorized export flows. Keep server state in TanStack Query, transient UI in Zustand, and authoritative calculation data on the server. Add accessibility and responsive regression coverage where practical.

Step 24: Complete the security, privacy, and governance release review. Re-test SSRF, DNS rebinding, redirects, prompt injection, hostile and oversized uploads, resource exhaustion, cross-user access, signed URL expiry, rate limits, session cookies, exact-origin CORS, forwarded-header trust, log/trace redaction, private bucket policy, secrets, and dependency vulnerabilities. Add automated secret and dependency scanning where it fits the existing GitHub Actions architecture. Document and implement upload and snapshot retention, deletion, sharing, correction, appeal, and high-impact allegation policies. Prevent automatic publication when stronger review is required. Never delete a snapshot referenced by a completed report and never place private evidence, credentials, prompts, or full uploads in logs or traces.

Step 25: Validate staging and operational readiness. Deploy the complete stack to staging with real Firebase Authentication, PostgreSQL/pgvector, Redis, private S3-compatible storage, Brave Search, DeepSeek, Sentry, and redacted LangSmith-compatible tracing where enabled. Make required staging and production smoke gates fail when their deployment URL is missing rather than silently skip. Validate controlled migrations, backup and restore, application rollback, reviewed migration rollback, API/worker revision compatibility, Redis restart recovery, SSE reconnect, queue retry/dead-job behavior, private signed downloads, bucket permissions, credential rotation, and alert delivery. Configure alerts for API errors, queue depth, run duration, provider failures, extraction failures, low evidence yield, citation-audit failures, abnormal cost, and security events. Run controlled live staging cases for each MVP input type without leaking private data.

Step 26: Run the final release audit. Re-read AGENTS.md, IMPLEMENTATION_PLAN.md, both project PDFs, and the Graphify knowledge graph. Verify that the full workflow reaches COMPLETED only after citation audit; citation failures cannot publish; all lint, type, test, migration, build, container, security, evaluation, deterministic end-to-end, and real staging gates pass; monitoring and alerts are active; policies are approved; and deployment/rollback are rehearsed. Run Graphify update after the final code changes. Produce a versioned release-readiness report containing commands, results, environment, known limitations, benchmark results, security evidence, operational evidence, and explicit blockers. State separately whether the project is approved for the controlled first shippable milestone and whether it is approved for public production launch. Do not call the project complete when only feature implementation is complete.
