###

Work in C:\Users\aliua\Elara.ai and follow the applicable root and nested AGENTS.md instructions. Query the existing Graphify graph first with a 1500-token budget, then read project-context/LITE_MODE_PLAN.md and only the directly relevant project guidance for the Lite step being implemented. Do not read the full implementation plan or project PDFs unless the Lite task explicitly requires a broad architecture review.

Keep the existing Full Mode production architecture intact. Do not remove, weaken, or replace FastAPI, Redis, Celery, PostgreSQL/pgvector, S3-compatible storage, Firebase Authentication, Brave Search, DeepSeek, LangGraph, or the Step 25B staging evidence. Lite Mode is an additive public demo path over a curated stored evidence library. It must not be described as the complete production verifier.

Use DeepSeek through server-side DEEPSEEK_* configuration only. Do not add OpenAI APIs, OpenAI environment variables, browser-side model calls, browser-side Supabase service-role access, or browser-side database writes. Supabase is allowed only for Lite Mode's curated public-demo corpus and Lite demo records; it does not replace the Full Mode PostgreSQL/FastAPI authorization boundary.

Lite Mode should be the first page users see at /. Full Mode must remain reachable through a clear button or navigation path to /verify. Lite Mode should look like the Full Mode interface and reuse existing app shell, UI components, report workspace patterns, source/evidence presentation, graph styling, and product language wherever practical.

Build Lite Mode first without depending on a cached library of Full Mode responses. Lite v1 should run as a real stored-corpus RAG demo over Supabase pgvector chunks with server-side DeepSeek synthesis and citation audit. The cached response library is deferred until after Full Mode is completed, temporarily hosted, exercised with controlled cases, and its 10-20 best completed reports are reviewed and captured for public demo use.

Lite model prompts must use the same compact contract style as Full Mode. Put
each model-assisted Lite stage in a small server-only module with a stable
PROMPT_VERSION, a short instruction-dense system prompt, typed structured output,
and separately assembled bounded request/context payloads. Keep the model to
language tasks only: classification, query planning, evidence comparison,
synthesis from selected chunks, and citation-entailment assistance. Retrieval,
thresholds, citation-presence checks, token budgets, final fallback decisions,
and any scoring or arithmetic remain deterministic code. Do not write verbose
conversational prompts, chain-of-thought requests, browser-side prompt builders,
or prompts that let retrieved text change policy.

Run focused checks first. Do not reinstall dependencies, rebuild all containers, or run repository-wide gates unless a Lite step explicitly requires it. If a real Supabase, Vercel, or DeepSeek credential is unavailable, implement the code path with placeholders, deterministic tests, and clear deployment documentation rather than blocking the build. After source or guidance changes, update Graphify. Report changed files, checks run, blockers, Lite feature completion, Full Mode milestone approval, and public production launch approval separately.

dev server startup: npm run --workspace @elara/web dev -- --port 3000

###

Lite Step 1: Add the Lite Mode environment contract and repository documentation. Update .env.example with placeholder-only Lite variables for NEXT_PUBLIC_ELARA_MODE, NEXT_PUBLIC_SUPABASE_URL if a browser-readable URL is needed, NEXT_PUBLIC_SUPABASE_ANON_KEY only if the browser must read public metadata, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_LITE_SCHEMA if needed, LITE_DEMO_ENABLED, LITE_MAX_QUERY_LENGTH, LITE_RETRIEVAL_LIMIT, LITE_MIN_SUPPORT_THRESHOLD, and LITE_CORPUS_VERSION. Do not add real secrets. Do not expose SUPABASE_SERVICE_ROLE_KEY, DEEPSEEK_API_KEY, database URLs, Firebase Admin credentials, Redis, Celery, S3, Sentry auth, Brave Search, or tracing credentials to the browser. Add a short docs or infrastructure note explaining that Lite Mode runs on Vercel plus Supabase pgvector while Full Mode remains the FastAPI/Redis/Celery verifier. Add tests or static checks that browser-facing env usage only references NEXT_PUBLIC_* values and that no Lite browser file imports the service-role client.

Lite Step 2: Create the Supabase Lite schema and pgvector setup documentation. Add a SQL file or migration-style runbook under infrastructure or a Lite-specific directory for enabling vector, creating lite_documents, lite_chunks, lite_runs, lite_run_citations, and optional lite_feedback and lite_eval_cases. Store document metadata, public source URL where available, publisher, document_date, ingestion timestamp, corpus version, chunk text, embedding vector, heading path, page/section/paragraph metadata, content hash, source citation label, generated answer metadata, cited sentence indexes, support status, and audit status. Include indexes for corpus version, document visibility, document date, content hash, vector similarity, and run created_at. Add row-level security policies that allow only safe public reads if required and deny public writes; privileged writes and retrieval should go through server-side code. Include a match_lite_chunks SQL function or equivalent query pattern for bounded vector retrieval plus metadata filters. Do not build or populate the cached Full Mode response library in this step; at most reserve a clearly deferred migration note for lite_cached_responses. Do not change the Full Mode Alembic migrations for this step unless a shared type contract truly requires it.

Lite Step 3: Add TypeScript schemas and shared Lite contracts. Create apps/web/lib/lite/schemas.ts or an equivalent module with Zod schemas and TypeScript types for LiteClaimRequest, LiteInputClassification, LiteQueryPlan, LiteRetrievedChunk, LiteEvidenceJudgment, LiteSelectedContext, LiteCitedSentence, LiteSynthesisOutput, LiteCitationAuditOutput, LiteAnswerResponse, LiteInsufficientEvidenceResponse, and LiteErrorResponse. Include corpus_version, reviewed_at, model metadata, prompt version, retrieval strategy, chunk ids, source labels, confidence/uncertainty fields, and audit status. Keep schemas compatible with existing report workspace concepts where practical so Lite output can be adapted to the Full Mode report UI. Add focused schema tests for valid answers, insufficient evidence, rejected citations, malformed requests, overlong input, and unknown corpus versions.

Lite Step 4: Add server-side Supabase and DeepSeek helpers for Lite Mode. Implement a server-only Supabase client that uses SUPABASE_SERVICE_ROLE_KEY only inside Next.js route handlers or server modules. Implement a Lite DeepSeek wrapper in TypeScript that uses DEEPSEEK_API_KEY, DEEPSEEK_BASE_URL, DEEPSEEK_CHAT_MODEL, DEEPSEEK_REASONING_MODEL, and optional DEEPSEEK_EMBEDDING_MODEL from server-side env only. Do not use OpenAI env names or SDKs. Support structured JSON outputs, deterministic low-temperature calls, timeout/error mapping, token/model metadata, and redacted logging. If a DeepSeek-compatible embedding route is unavailable, provide a clearly named lexical/metadata fallback and keep the vector schema ready. Add tests proving missing required server credentials fail closed in production-like Lite execution but allow deterministic mocked tests.

Lite Step 5: Build the curated corpus ingestion path. Add scripts or server-side utilities to ingest a small approved evidence library into Supabase Lite tables. The ingestion path should accept structured local fixtures or markdown/json source records, split content into chunks by headings, paragraphs, table rows, transcript turns, and semantic boundaries with limited overlap, preserve page/section/paragraph metadata, compute content hashes, and generate embeddings through the approved DeepSeek-compatible path when configured. Include a deterministic no-provider fixture mode for tests. Do not ingest arbitrary user uploads, perform open-web crawling for Lite Mode, or populate cached Full Mode responses yet. Add seed examples that are safe to publish and demonstrate support, contradiction, insufficient evidence, quote/paraphrase handling, and numerical context.

Lite Step 6: Implement Lite retrieval over the stored evidence library. Build a server-side retrieval helper that validates the request, creates one or more query strings from the input, embeds the query when available, searches Supabase pgvector chunks, applies corpus version and visibility filters, combines semantic similarity with lexical matching and metadata fit, boosts exact names, quotations, numbers, dates, and identifiers, deduplicates near-identical chunks, and returns a bounded set of LiteRetrievedChunk records. Retrieval must never decide truth. It should mark low-confidence retrievals and preserve exact chunk text and source metadata for citation. Add focused tests for vector retrieval mocks, lexical fallback, exact quote/number matching, corpus filters, empty retrieval, duplicate chunks, and metadata preservation.

Lite Step 7: Implement the Lite multi-agent RAG pipeline. Create server-side orchestration helpers for intake classification, query planning, retrieval, evidence judgment, context selection, answer synthesis, and citation audit. Before orchestration, add Full Mode-style prompt contract modules for each model-assisted Lite stage: version constants, compact system prompts, typed Zod output schemas, bounded context builders, low-temperature structured calls, token-usage metadata, and tests or static checks that reject oversized prompt/context payloads. The evidence judge should label chunks as supports, contradicts, background, or irrelevant and explain only with evidence-safe public rationale. The synthesis step must answer only from selected chunks and attach citation ids to factual sentences. The citation audit must verify citation presence, cited chunk existence, and sentence-to-chunk support; unsupported factual sentences must be revised or removed. Keep retrieval, context limits, thresholds, fallback status, score-like labels, and citation-presence checks deterministic. If support is weak, missing, or only background context, return an insufficient-evidence response. Add tests for successful cited answer, contradiction-aware answer, insufficient evidence, unsupported sentence rejection, malformed model output, audit revision, prompt-version metadata, and prompt/context budget enforcement.

Lite Step 8: Add the Lite API route. Implement apps/web/app/api/lite/answer/route.ts or an equivalent Next.js App Router route. The route should accept POST requests, validate the input with Lite schemas, enforce length and simple rate/abuse limits suitable for the public demo, run the Lite multi-agent RAG pipeline, and return a typed LiteAnswerResponse or LiteInsufficientEvidenceResponse. The route must not call Full Mode FastAPI, Redis, Celery, Brave Search, S3, or Firebase Admin. It must not expose private prompts, chain-of-thought, provider credentials, or Supabase service-role details. Persist non-sensitive lite_runs and lite_run_citations when Supabase is configured; otherwise allow deterministic local fixtures for tests. Add route tests for success, insufficient evidence, bad input, missing corpus, provider failure, and credential boundary behavior.

Lite Step 9: Make Lite Mode the default public page. Convert apps/web/app/page.tsx from the Full Mode-first workspace into the Lite evidence-library workspace while preserving the existing visual language. The first viewport should contain the Lite claim/question form, a restrained Lite label, evidence-library scope text, and report/progress affordances that match the Full Mode UI. Include a clear button or navigation item to /verify for Full Mode, such as "Open Full Verifier". Do not make a marketing landing page. Do not add visible instructional text about implementation details, keyboard shortcuts, or hidden architecture. Keep all sensitive credentials out of browser code. Add focused tests or snapshots for default route rendering, Full Mode link presence, Lite label presence, and absence of secret env names in rendered output.

Lite Step 10: Reuse the Full Mode report workspace for Lite output. Add a mode-aware adapter that maps LiteAnswerResponse, LiteRetrievedChunk, LiteEvidenceJudgment, and LiteCitationAuditOutput into the existing report workspace shape wherever practical. The Lite report should show cited answer, evidence cards, exact source chunks, source metadata, support/contradiction labels, uncertainty, methodology/version details, corpus version, model/prompt version, and "Evidence reviewed as of..." timestamp. It should visually match Full Mode reports while clearly stating that the evidence came from the curated Lite library. Do not display final Full Mode numerical verdicts unless the Lite pipeline actually computes them deterministically. Add component tests for cited sentences, source drawer/chunk display, insufficient-evidence state, mobile layout, and Full Mode visual parity.

Lite Step 11: Add Lite progress and demo run UX without Redis or Celery. Build a request-local or client-side progress timeline that mirrors the Full Mode stage language without claiming background worker execution. Stages may include intake, query planning, library retrieval, evidence review, synthesis, and citation audit. If using a single request, show optimistic/progressive local state while the server returns the final typed response. Do not use fake claims that Redis or Celery are running in Lite Mode. Keep the Full Mode live SSE path unchanged. Add tests for loading, success, failure, retry, and cancellation/clear behavior in the Lite UI.

Lite Step 12: Add Lite security, privacy, and boundary tests. Create focused tests that prove DeepSeek and Supabase service-role credentials are server-side only, browser code never imports server-only Lite clients, the Lite route rejects overlong or malformed input, unsupported answers are not published as supported, citations must reference existing chunks, and public Supabase policies do not allow unsafe writes. Include checks that Lite Mode does not call Brave Search, Full Mode FastAPI, Redis, Celery, S3, Firebase Admin, or object-storage credentials. Run the narrowest relevant frontend lint/type/test commands and any focused route tests. Do not run full API, worker, container, or staging gates unless a shared contract was changed.

Lite Step 13: Add Lite deployment and portfolio documentation. Document how to deploy the public Lite demo on Vercel with Supabase pgvector, including required environment variable names, Supabase SQL setup, seed corpus ingestion, DeepSeek server-side configuration, public demo limitations, and the difference between Lite Mode and Full Mode. Document that Full Mode remains paused after Step 25B and requires paid always-on infrastructure for the FastAPI/Redis/Celery worker stack. Add README copy that honestly presents the public demo as stored-corpus cited RAG and the repository as containing the full production verifier architecture. Explicitly state that Lite v1 ships without the cached Full Mode response library; the cache will be populated only after Full Mode is completed and reviewed outputs are captured. Include a later temporary Full Mode capture checklist: architecture diagram, queue/worker screenshots with secrets hidden, 10-20 high-quality completed reports, smoke evidence, and walkthrough video.

Lite Step 14: Perform Lite v1 release polish and readiness review. Verify the Lite home page, API route, report view, evidence citations, insufficient-evidence path, Full Mode link, responsive behavior, accessibility basics, and Vercel/Supabase deployment documentation. Run the focused Lite gate set: frontend lint, TypeScript check, Lite schema/route/component tests, and a production Next.js build if dependencies and environment permit. Confirm no secrets are exposed in rendered output or client bundles. Confirm that the cached Full Mode response library is not required for Lite v1 readiness. Update Graphify. State separately whether Lite public demo is ready, whether Full Mode first-shippable milestone is approved, and whether public production launch is approved. Do not mark Full Mode complete or production-approved because Lite Mode passes.

Lite Step 15: After Lite v1 is hosted, resume and complete Full Mode before building the cached response library. Use the existing full-version prompts and plan for FastAPI, Redis, Celery, PostgreSQL/pgvector, S3-compatible storage, Brave, DeepSeek, LangGraph workflow closure, staging validation, and release evidence. Temporarily host Full Mode only when needed to validate and record behavior. Run controlled cases across the most compelling use cases and capture sanitized operational proof without exposing secrets, private prompts, bearer tokens, signed URLs, full uploads, or sensitive source passages. Do not add cached responses from incomplete, unaudited, failed, or test-only Full Mode runs.

Lite Step 16: Populate the Lite cached response library from reviewed Full Mode outputs. Add a Lite cache schema or migration for lite_cached_responses if it was not created earlier, plus an import script that accepts reviewed Full Mode report exports. Store the submitted prompt, normalized claim/question, final answer, exact citations, safe evidence metadata, source snapshot/public-source references where safe, reviewed_at timestamp, model/prompt/workflow/methodology/parser/retrieval/scoring versions, demo tags, and redaction review status. Import only 10-20 high-quality Full Mode exemplars that are safe to show publicly. Lite may use these cached examples for suggested prompts or exact/near-exact demo matches, but cached examples must be labeled as reviewed demo examples and not presented as newly generated live verification runs. Preserve the live stored-corpus RAG path as the fallback for unmatched inputs. Add tests for cache matching, cache miss fallback, cache labeling, redaction metadata, and citation display.
